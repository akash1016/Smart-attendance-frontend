import React from 'react';import AdminPanel from './AdminPanel';import TeacherPanel from './TeacherPanel';import StudentPanel from './StudentPanel';export default function Dashboard(){const role=localStorage.getItem('role');const user=localStorage.getItem('username');return (<div><h2>Welcome {user} ({role})</h2>{role==='admin'&&<AdminPanel/>}{role==='teacher'&&<TeacherPanel/>}{role==='student'&&<StudentPanel/>}<hr/><p style={{color:'#666'}}>Built with OpenCV LBPH face recognizer (no dlib required).</p></div>)}